# v2.6.7 构建 260911001 TestFlight 验收

## 发布身份

- 版本保持 `2.6.7`，基础构建号由 `260910002` 升至 `260911001`。
- Apple 构建 ID：`60ab32bb-fb5d-46dc-90e6-aab8f4b38029`。
- Bundle ID：`com.niki.xxread`。
- 更新日志由当前工作区相对上一 TestFlight 构建 `260910002` 的差异整理，已同步 `CHANGELOG.md`、`.github/release-notes/v2.6.7+260911001.md` 和 `assets/changelog/changelog.json`。

## TestFlight

- 本机签名归档后上传，版本 `2.6.7`、构建 `260911001`。
- 归档身份、非豁免加密标记、隐私清单、签名与 provisioning profile 检查通过。
- 使用本机 Xcode 27.0（27A266a）和 iPhoneOS SDK 27.0；Apple 上传于 2026-09-11 01:46:35（Asia/Shanghai）成功。此记录仅用于 TestFlight 测试分发，不代表 App Store 正式审核或上架。
- Apple 后台处理状态已确认为 `VALID`，内部及外部状态均为 `IN_BETA_TESTING`；`autoNotifyEnabled=true`。
- 已保存 `zh-Hans` 与 `en-US` 测试内容，加入现有 `out` 外部测试组；Beta App Review 已回读为 `APPROVED`。通过 API 确认组内包含新构建、测试内容与提交文本一致。
- [TestFlight 测试入口](https://testflight.apple.com/join/usRgjzQD)。

## 本构建说明

- 发现页会先展示已加载完成的书源，刷新时保留现有内容。
- 漫画连续阅读时，图片加载不再把正在看的位置顶走。
- 漫画目录改为阅读主题样式，打开后定位到当前章节。
- 阅读搜索跳转更准确；横滑翻页在滑动结束后再确认进度。
- 网络异常时不再误退出登录，账号页可以直接重试。
- 书源开关和收藏保存失败时会提示，并恢复原来的状态。

## 验证边界

- 本次未创建 GitHub Release 标签，也未发布跨平台安装包。
- 未完成各实体设备安装升级、发现页慢源、漫画长章节和弱网登录的真机验收。
- 归档期间工作区另有 macOS 商店构建工具等并行修改，未纳入本构建说明，也不代表已随本次 TestFlight 单独验收。
